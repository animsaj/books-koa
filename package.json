{
  "name": "books-koa",
  "version": "1.0.0",
  "description": "",
  "main": "bin/index.js",
  "scripts": {
    "watch": "babel -w src/ -d bin/",
    "build": "babel src/ -d bin/",
    "serve": "babel -w src/ -d bin/ | nodemon --watch bin",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "animsaj",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "babel-preset-stage-0": "^6.22.0",
    "koa": "^2.2.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.23.0",
    "koa-bodyparser": "^3.2.0",
    "koa-router": "^7.0.1",
    "mongoose": "^4.9.1"
  }
}
